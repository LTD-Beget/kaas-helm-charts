apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./base.yaml

patches:
  - path: ./deployment.dex.yaml
    target:
      kind: Deployment
      name: dex

  - path: ./service.dex.yaml
    target:
      kind: Service
      name: dex
