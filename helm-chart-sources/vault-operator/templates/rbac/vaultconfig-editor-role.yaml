{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "vault-operator.labels" . | nindent 4 }}
  name: {{ include "vault-operator.resourceName" (dict "suffix" "vaultconfig-editor-role" "context" $) }}
rules:
  - apiGroups:
      - vault.in-cloud.io
    resources:
      - vaultconfigs
    verbs:
      - create
      - delete
      - get
      - list
      - patch
      - update
      - watch
  - apiGroups:
      - vault.in-cloud.io
    resources:
      - vaultconfigs/status
    verbs:
      - get
{{- end }}
