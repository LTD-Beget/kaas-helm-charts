{{- if and .Values.addons.helminserter.enabled .Values.addons.system.enabled }}
  {{- $clientContext := set (deepCopy .) "Values" (set (deepCopy .Values) "environment" "client") -}}
  
  {{- include "helm-inserter.addon" . }}

  {{- .Files.Get "files/systemAddons/helm-inserter/values.yml" | nindent 0 }}

  {{- include "helm-inserter.phase" . }}
{{- end }}