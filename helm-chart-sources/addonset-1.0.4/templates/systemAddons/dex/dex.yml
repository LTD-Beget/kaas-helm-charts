{{- if .Values.addons.dex.enabled }}
  {{- $clientContext := set (deepCopy .) "Values" (set (deepCopy .Values) "environment" "client") -}}
  
  {{- if .Values.addons.system.enabled }}
    {{- include "dex.addon" . }}
    {{- .Files.Get "files/systemAddons/dex/values-system.yml" | nindent 0 }}
  {{- end }}

  {{- .Files.Get "files/systemAddons/dex/values.yml" | nindent 0 }}

  {{- include "dex.phase" . }}

  {{- if eq .Values.environment "infra" }}
    {{- include "dex.phase" $clientContext }}
  {{- end }}
{{- end }}