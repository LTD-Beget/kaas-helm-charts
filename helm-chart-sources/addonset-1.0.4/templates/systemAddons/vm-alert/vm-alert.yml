{{- if .Values.addons.vmalert.enabled }}
  {{- $clientContext := set (deepCopy .) "Values" (set (deepCopy .Values) "environment" "client") -}}
  
  {{- if .Values.addons.system.enabled }}
    {{- include "vm-alert.addon" . }}
  {{- end }}

  {{- .Files.Get "files/systemAddons/vm-alert/values.yml" | nindent 0 }}

  {{- include "vm-alert.phase" . }}

  {{- if eq .Values.environment "infra" }}
    {{- include "vm-alert.phase" $clientContext }}
  {{- end }}
{{- end }}