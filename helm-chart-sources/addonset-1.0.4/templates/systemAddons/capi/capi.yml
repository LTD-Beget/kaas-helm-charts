{{- if and .Values.addons.capi.enabled .Values.addons.system.enabled }}
  {{- $clientContext := set (deepCopy .) "Values" (set (deepCopy .Values) "environment" "client") -}}
  
  {{- include "capi.addon" . }}

  {{- .Files.Get "files/systemAddons/capi/values.yml" | nindent 0 }}

  {{- include "capi.phase" . }}
{{- end }}