{{- $argocd := index .Values "argo-cd" -}}

{{- if and (gt ($argocd.server.replicas | int) 0) .Values.monitoring.secureService.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Release.Name }}-server-metrics
  namespace: {{ .Release.Namespace }}
spec:
  secretName: argocd-server-metrics
  duration: 8760h
  renewBefore: 720h
  commonName: {{ .Release.Name }}-server-metrics
  dnsNames:
    - {{ .Release.Name }}-server-metrics
    - {{ .Release.Name }}-server-metrics.{{ .Release.Namespace }}
    - {{ .Release.Name }}-server-metrics.{{ .Release.Namespace }}.svc
  privateKey:
    algorithm: RSA
    size: 2048
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    kind: {{ .Values.monitoring.secureService.issuer.kind }}
    name: {{ .Values.monitoring.secureService.issuer.name }}
{{- end }}
