{{- $argocd := index .Values "argo-cd" -}}

{{- if and (gt ($argocd.repoServer.replicas | int) 0) .Values.monitoring.secureService.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Release.Name }}-repo-server-metrics
  namespace: {{ .Release.Namespace }}
spec:
  secretName: argocd-repo-server-metrics
  duration: 8760h
  renewBefore: 720h
  commonName: {{ .Release.Name }}-repo-server-metrics
  dnsNames:
    - {{ .Release.Name }}-repo-server-metrics
    - {{ .Release.Name }}-repo-server-metrics.{{ .Release.Namespace }}
    - {{ .Release.Name }}-repo-server-metrics.{{ .Release.Namespace }}.svc
  privateKey:
    algorithm: RSA
    size: 2048
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    kind: {{ .Values.monitoring.secureService.issuer.kind }}
    name: {{ .Values.monitoring.secureService.issuer.name }}
{{- end }}
