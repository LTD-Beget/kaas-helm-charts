{{- if .Values.monitoring.secureService.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Release.Name }}-applicationset-controller-metrics
  namespace: {{ .Release.Namespace }}
spec:
  secretName: argocd-applicationset-controller-metrics
  duration: 8760h
  renewBefore: 720h
  commonName: {{ .Release.Name }}-applicationset-controller-metrics
  dnsNames:
    - {{ .Release.Name }}-applicationset-controller-metrics
    - {{ .Release.Name }}-applicationset-controller-metrics.{{ .Release.Namespace }}
    - {{ .Release.Name }}-applicationset-controller-metrics.{{ .Release.Namespace }}.svc
  privateKey:
    algorithm: RSA
    size: 2048
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    kind: {{ .Values.monitoring.secureService.issuer.kind }}
    name: {{ .Values.monitoring.secureService.issuer.name }}
{{- end }}
