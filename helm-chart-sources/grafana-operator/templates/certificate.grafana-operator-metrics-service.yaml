{{- if .Values.monitoring.secureService.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Release.Name }}-metrics-service-svc-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: grafana-operator-metrics-service-svc-tls
  duration: 8760h
  renewBefore: 720h
  commonName: {{ .Release.Name }}-metrics-service
  dnsNames:
    - {{ .Release.Name }}-metrics-service
    - {{ .Release.Name }}-metrics-service.{{ .Release.Namespace }}
    - {{ .Release.Name }}-metrics-service.{{ .Release.Namespace }}.svc
  privateKey:
    algorithm: RSA
    size: 2048
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    kind: {{ .Values.monitoring.secureService.issuer.kind }}
    name: {{ .Values.monitoring.secureService.issuer.name }}
{{- end }}
