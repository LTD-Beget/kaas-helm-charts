grafana-operator: {}
# some values for coredns

companyPrefix: example

networkPolicies:
  enabled: false

monitoring:
  enabled: false
  type: VictoriaMetrics
  namespace: default
  joblabel: jobLabel
  secureService:
    enabled: false
    # Create a secret containing both 'tls.crt' and 'tls.key' fields
    issuer:
      kind: ClusterIssuer
      name: default
