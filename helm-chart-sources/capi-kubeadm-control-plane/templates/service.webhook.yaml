---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "capi-kubeadm-control-plane.fullname" . }}-webhook-service
  labels:
    {{- include "capi-kubeadm-control-plane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.webhookService.type }}
  ports:
    - port: {{ .Values.webhookService.port }}
      targetPort: webhook-server
      protocol: TCP
      name: webhook-server
  selector:
    {{- include "capi-kubeadm-control-plane.selectorLabels" . | nindent 4 }}
