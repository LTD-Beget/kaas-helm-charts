---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bcloud-capi-kubeadm-control-plane.fullname" . }}-metrics
  labels:
    {{- include "bcloud-capi-kubeadm-control-plane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metricsService.type }}
  ports:
    - port: {{ .Values.metricsService.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "bcloud-capi-kubeadm-control-plane.selectorLabels" . | nindent 4 }}
