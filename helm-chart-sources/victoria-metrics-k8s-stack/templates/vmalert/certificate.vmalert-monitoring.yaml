{{- $vmstack := index .Values "victoria-metrics-k8s-stack" -}}

{{- if and .Values.monitoring.secureService.enabled $vmstack.vmalert.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vmalert-monitoring-svc-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: vmalert-monitoring-svc-tls
  duration: 8760h
  renewBefore: 720h
  commonName: vmalert
  dnsNames:
    - vmalert
    - vmalert.{{ .Release.Namespace }}
    - vmalert.{{ .Release.Namespace }}.svc
  privateKey:
    algorithm: RSA
    size: 2048
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    kind: {{ .Values.monitoring.secureService.issuer.kind }}
    name: {{ .Values.monitoring.secureService.issuer.name }}
{{- end }}
