{{- $vmstack := index .Values "victoria-metrics-k8s-stack" -}}

{{- if and .Values.monitoring.secureService.enabled $vmstack.vmagent.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Release.Name }}-vmagent-monitoring-svc-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: vmagent-monitoring-svc-tls
  duration: 8760h
  renewBefore: 720h
  commonName: {{ .Release.Name }}-vmagent-monitoring
  dnsNames:
    - {{ .Release.Name }}-vmagent-monitoring
    - {{ .Release.Name }}-vmagent-monitoring.{{ .Release.Namespace }}
    - {{ .Release.Name }}-vmagent-monitoring.{{ .Release.Namespace }}.svc
  privateKey:
    algorithm: RSA
    size: 2048
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    kind: {{ .Values.monitoring.secureService.issuer.kind }}
    name: {{ .Values.monitoring.secureService.issuer.name }}
{{- end }}
