{{- if .Values.tls.vmAgent.issuer.enabled }}
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ .Values.tls.vmAgent.issuer.name }}
spec:
  ca:
    secretName: {{ .Values.tls.vmAgent.issuer.secretName }}
{{- end }}