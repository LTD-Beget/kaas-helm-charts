{{- $vmstack := index .Values "victoria-metrics-k8s-stack" -}}

{{- if and .Values.monitoring.secureService.enabled $vmstack.vmauth.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vmauth-monitoring-svc-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: vmauth-monitoring-svc-tls
  duration: 8760h
  renewBefore: 720h
  commonName: vmauth
  dnsNames:
    - vmauth
    - vmauth.{{ .Release.Namespace }}
    - vmauth.{{ .Release.Namespace }}.svc
  privateKey:
    algorithm: RSA
    size: 2048
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    kind: {{ .Values.monitoring.secureService.issuer.kind }}
    name: {{ .Values.monitoring.secureService.issuer.name }}
{{- end }}
