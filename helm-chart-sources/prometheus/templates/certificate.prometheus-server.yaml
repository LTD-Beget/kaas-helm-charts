{{- if .Values.monitoring.secureService.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Release.Name }}-server-svc-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: prometheus-server-svc-tls
  duration: 8760h
  renewBefore: 720h
  commonName: {{ .Release.Name }}-server
  dnsNames:
    - {{ .Release.Name }}-server
    - {{ .Release.Name }}-server.{{ .Release.Namespace }}
    - {{ .Release.Name }}-server.{{ .Release.Namespace }}.svc
  privateKey:
    algorithm: RSA
    size: 2048
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    kind: {{ .Values.monitoring.secureService.issuer.kind }}
    name: {{ .Values.monitoring.secureService.issuer.name }}
{{- end }}
