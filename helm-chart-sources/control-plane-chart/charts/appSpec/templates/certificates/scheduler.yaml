{{- range $appName, $appValue := .Values.applications }}
  {{- if and $appValue.enabled (not (dig "csiDriver" false $appValue)) }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: scheduler
  namespace: beget-system
spec:
  secretName: scheduler-server-tls
  duration: 8760h
  renewBefore: 1460h
  commonName: system:kube-scheduler-server
  dnsNames:
    {{- range $dns := $appValue.containers.kubeScheduler.certSANs }}
    - {{ tpl $dns $ }}
    {{- end }}
  ipAddresses:
    {{- range $ip := $appValue.containers.kubeScheduler.certIPs }}
    - {{ $ip | quote }}
    {{- end }}
  privateKey:
    algorithm: RSA
    size: 2048
  usages:
    - key encipherment
    - data encipherment
    - server auth
  issuerRef:
    name: kubernetes-ca
    kind: Issuer
  {{- end }}
{{- end }}