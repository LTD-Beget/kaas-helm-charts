apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./base.yaml

patches:
  - path: ./deployment.kube-state-metrics.yaml
    target:
      kind: Deployment
      name: kube-state-metrics

  - path: ./service.kube-state-metrics.yaml
    target:
      kind: Service
      name: kube-state-metrics
