{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: {{ include "cluster-claim-operator.name" . }}
        helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    name: {{ include "cluster-claim-operator.resourceName" (dict "suffix" "clusterclaimobserveresourcetemplate-viewer-role" "context" $) }}
rules:
    - apiGroups:
        - clusterclaim.in-cloud.io
      resources:
        - clusterclaimobserveresourcetemplates
      verbs:
        - get
        - list
        - watch
    - apiGroups:
        - clusterclaim.in-cloud.io
      resources:
        - clusterclaimobserveresourcetemplates/status
      verbs:
        - get
{{- end }}
