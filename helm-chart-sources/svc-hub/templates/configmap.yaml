apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "svc-hub.configmap" . }}
  labels:
    {{ (include "svc-hub.labels" .) | nindent 4 }}
data:
  hubconfig.yml: |
    {{ .Values.hub.config | toYaml | nindent 4 }}    
