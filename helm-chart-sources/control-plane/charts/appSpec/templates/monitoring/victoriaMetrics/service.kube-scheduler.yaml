{{- range $appName, $appValue := .Values.applications }}
  {{- if $appValue.enabled }}
    {{- if and $appValue.monitoring.enabled (eq $appValue.monitoring.type "VictoriaMetrics") }}
---
apiVersion: v1
kind: Service
metadata:
  name: "{{ $.Release.Name }}-scheduler-monitoring"
  namespace: {{ $.Release.Namespace }}
  labels:
    monitoring.in-cloud.io/service: "{{ $.Release.Name }}-scheduler-monitoring"
    jobLabel: scheduler
spec:
  ports:
    - name: https-metrics
      port: {{ index $appValue.containers.kubeScheduler.extraArgs "secure-port" | default "20259" }}
      protocol: TCP
      targetPort: {{ index $appValue.containers.kubeScheduler.extraArgs "secure-port" | default "20259" }}
  selector:
    app.kubernetes.io/name: control-plane
  type: ClusterIP
    {{- end }}
  {{- end }}
{{- end }}
