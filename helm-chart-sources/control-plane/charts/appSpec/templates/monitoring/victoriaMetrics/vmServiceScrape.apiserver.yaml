{{- range $appName, $appValue := .Values.applications }}
  {{- if $appValue.enabled }}
    {{- if and $appValue.monitoring.enabled (eq $appValue.monitoring.type "VictoriaMetrics") }}
    {{- $defaultServerName := "client-cp-control-plane" }}
    {{- $serverName := "" }}
    {{- if $appValue.csiDriver }}
    {{- $serverName = index (split (dig "controlPlane" "volumes" "apiserver-csi" "volume" "volumeAttributes" "csi.cert-manager.io/dns-names" $defaultServerName $appValue) ",") 0 }}
    {{- else }}
    {{- $serverName = dig "controlPlane" "containers" "kubeApiserver" "certSANs" 0 $defaultServerName $appValue }}
    {{- end }}

---
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: "{{ $.Release.Name }}-apiserver"
  namespace: {{ $.Release.Namespace }}
spec:
  endpoints:
    - port: https-metrics
      scheme: "https"
      tlsConfig:
        serverName: "{{ $serverName }}"
        ca:
          secret:
            name: "{{ $appValue.clusterName }}-ca"
            key: ca.crt
  selector:
    matchLabels:
      monitoring.in-cloud.io/service: "{{ $.Release.Name }}-apiserver-monitoring"
  jobLabel: jobLabel
    {{- end }}
  {{- end }}
{{- end }}
