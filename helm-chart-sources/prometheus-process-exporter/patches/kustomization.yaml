apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./base.yaml

patches:
  - path: ./daemonset.prometheus-process-exporter.yaml
    target:
      kind: DaemonSet
      name: prometheus-process-exporter
