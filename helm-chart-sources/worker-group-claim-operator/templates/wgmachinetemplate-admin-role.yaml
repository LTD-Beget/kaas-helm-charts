{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: {{ include "worker-group-claim-operator.name" . }}
        helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    name: {{ include "worker-group-claim-operator.resourceName" (dict "suffix" "wgmachinetemplate-admin-role" "context" $) }}
rules:
    - apiGroups:
        - workergroup.in-cloud.io
      resources:
        - wgmachinetemplates
      verbs:
        - '*'
    - apiGroups:
        - workergroup.in-cloud.io
      resources:
        - wgmachinetemplates/status
      verbs:
        - get
{{- end }}
