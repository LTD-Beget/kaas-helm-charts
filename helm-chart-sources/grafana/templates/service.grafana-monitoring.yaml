apiVersion: v1
kind: Service
metadata:
  labels:
    monitoring.in-cloud.io/service: grafana
  name: grafana-monitoring
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: https-metrics
      port: 11043
      protocol: TCP
      targetPort: https-metrics
  selector:
    app: grafana
