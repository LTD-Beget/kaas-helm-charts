{{- if .Values.monitoring.secureService.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Release.Name }}-operator-svc-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: cilium-operator-svc-tls
  duration: 8760h
  renewBefore: 720h
  commonName: {{ .Release.Name }}-operator
  dnsNames:
    - {{ .Release.Name }}-operator
    - {{ .Release.Name }}-operator.{{ .Release.Namespace }}
    - {{ .Release.Name }}-operator.{{ .Release.Namespace }}.svc
  privateKey:
    algorithm: RSA
    size: 2048
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    kind: {{ .Values.monitoring.secureService.issuer.kind }}
    name: {{ .Values.monitoring.secureService.issuer.name }}
{{- end }}
