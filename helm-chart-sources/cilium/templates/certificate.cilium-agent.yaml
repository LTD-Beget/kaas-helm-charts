{{- if .Values.monitoring.secureService.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Release.Name }}-agent-monitoring-svc-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: cilium-agent-svc-tls
  duration: 8760h
  renewBefore: 720h
  commonName: {{ .Release.Name }}-agent-monitoring
  dnsNames:
    - {{ .Release.Name }}-agent-monitoring
    - {{ .Release.Name }}-agent-monitoring.{{ .Release.Namespace }}
    - {{ .Release.Name }}-agent-monitoring.{{ .Release.Namespace }}.svc
  privateKey:
    algorithm: RSA
    size: 2048
  usages:
    - digital signature
    - key encipherment
    - server auth
  issuerRef:
    kind: {{ .Values.monitoring.secureService.issuer.kind }}
    name: {{ .Values.monitoring.secureService.issuer.name }}
{{- end }}
