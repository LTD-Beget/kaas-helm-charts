trust-manager: {}

monitoring:
  enabled: false
  type: VictoriaMetrics
  namespace: default

bundle:
  enabled: false
  name: "ca.crt"
  # опционально
  labels: {}
  annotations: {}
  sourceSecret:
    name: "default-crt"
    key: "tls.crt"
  extraSecretSources: []
  # - name: "another-ca-secret"
  #   namespace: "trust"
  #   key: "tls.crt"

  target:
    configMapKey: "ca.crt"
    namespaceSelector:
      matchLabels:
        default.com/default: "default"
