{{- if .Values.monitoring.vmServiceScrape.enabled }}
---
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: {{ include "bcloud-capi-kubeadm-bootstrap.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "bcloud-capi-kubeadm-bootstrap.labels" . | nindent 4 }}
spec:
  endpoints:
  - path: /metrics
    port: metrics
    scheme: HTTPS
    bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
    tlsConfig:
      serverName: localhost
  {{- if not .Values.certificates.useExternalIssuer }}
      insecureSkipVerify: true
  {{- end }}
  jobLabel: {{ include "bcloud-capi-kubeadm-bootstrap.fullname" . }}
  selector:
    matchLabels:
    {{- include "bcloud-capi-kubeadm-bootstrap.selectorLabels" . | nindent 4 }}
{{- end }}
