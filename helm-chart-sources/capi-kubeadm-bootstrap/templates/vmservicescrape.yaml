{{- if .Values.monitoring.vmServiceScrape.enabled }}
---
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: {{ include "capi-kubeadm-bootstrap.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "capi-kubeadm-bootstrap.labels" . | nindent 4 }}
spec:
  endpoints:
  - path: /metrics
    port: metrics
    scheme: HTTPS
    bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
    tlsConfig:
      serverName: localhost
  {{- if not .Values.certificates.useExternalIssuer }}
      insecureSkipVerify: true
  {{- end }}
  jobLabel: {{ include "capi-kubeadm-bootstrap.fullname" . }}
  selector:
    matchLabels:
      monitoring.in-cloud.io/service: {{ include "capi-kubeadm-bootstrap.fullname" . }}-metrics-service
      {{- include "capi-kubeadm-bootstrap.selectorLabels" . | nindent 6 }}
{{- end }}
