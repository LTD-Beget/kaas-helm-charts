---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bcloud-capi-kubeadm-bootstrap.fullname" . }}-metrics-service
  labels:
    {{- include "bcloud-capi-kubeadm-bootstrap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metricsService.type }}
  ports:
    - port: {{ .Values.metricsService.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "bcloud-capi-kubeadm-bootstrap.selectorLabels" . | nindent 4 }}
