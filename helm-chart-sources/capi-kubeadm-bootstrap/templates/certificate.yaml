---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "capi-kubeadm-bootstrap.fullname" . }}-serving-cert
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "capi-kubeadm-bootstrap.labels" . | nindent 4 }}
spec:
  dnsNames:
    - {{ include "capi-kubeadm-bootstrap.fullname" . }}-webhook-service
    - {{ include "capi-kubeadm-bootstrap.fullname" . }}-webhook-service.{{ .Release.Namespace }}
    - {{ include "capi-kubeadm-bootstrap.fullname" . }}-webhook-service.{{ .Release.Namespace }}.svc
    - {{ include "capi-kubeadm-bootstrap.fullname" . }}-webhook-service.{{ .Release.Namespace }}.svc.{{ .Values.webhookService.domain }}
    - {{ include "capi-kubeadm-bootstrap.fullname" . }}-metrics-service
    - {{ include "capi-kubeadm-bootstrap.fullname" . }}-metrics-service.{{ .Release.Namespace }}
    - {{ include "capi-kubeadm-bootstrap.fullname" . }}-metrics-service.{{ .Release.Namespace }}.svc
    - {{ include "capi-kubeadm-bootstrap.fullname" . }}-metrics-service.{{ .Release.Namespace }}.svc.{{ .Values.webhookService.domain }}
  issuerRef:
  {{- if .Values.certificates.useExternalIssuer }}
    {{ .Values.certificates.externalIssuer | toYaml | nindent 4 }}
  {{- else }}
    kind: Issuer
    name: {{ include "capi-kubeadm-bootstrap.fullname" . }}-selfsigned-issuer
  {{- end }}
  secretName: {{ include "capi-kubeadm-bootstrap.fullname" . }}-webhook-service-cert
  subject:
    organizations:
      - k8s-sig-cluster-lifecycle
