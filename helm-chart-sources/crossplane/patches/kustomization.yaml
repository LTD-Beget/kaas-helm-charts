apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./base.yaml

patches:
  - path: ./deployment.crossplane.yaml
    target:
      kind: Deployment
      name: crossplane

  - path: ./deployment.crossplane.yaml
    target:
      kind: Deployment
      name: crossplane-rbac-manager
