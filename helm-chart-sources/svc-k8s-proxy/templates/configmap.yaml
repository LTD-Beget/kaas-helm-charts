apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "svc-k8s-proxy.fullname" . }}
  labels:
    {{- include "svc-k8s-proxy.labels" . | nindent 4 }}
data:
  config: |
    {{ .Values.config | toYaml | nindent 4 }}
