{{- if and .Values.addons.crossplanecompositions.enabled .Values.addons.system.enabled}}
  {{- $clientContext := set (deepCopy .) "Values" (set (deepCopy .Values) "environment" "client") -}}

  {{- include "crossplane-compositions.addon" . }}

  {{- .Files.Get "files/systemAddons/crossplane-compositions/values.yml" | nindent 0 }}

{{- end }}
