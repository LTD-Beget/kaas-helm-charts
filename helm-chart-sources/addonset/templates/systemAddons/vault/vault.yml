{{- if .Values.addons.vault.enabled }}
  {{- $clientContext := set (deepCopy .) "Values" (set (deepCopy .Values) "environment" "client") -}}
  
  {{- if .Values.addons.system.enabled }}
    {{- include "vault.addon" . }}
    {{- .Files.Get "files/systemAddons/vault/values-system.yml" | nindent 0 }}
  {{- end }}

  {{- .Files.Get "files/systemAddons/vault/values.yml" | nindent 0 }}

  {{- include "vault.phase" . }}

  {{- if eq .Values.environment "infra" }}
    {{- include "vault.phase" $clientContext }}
  {{- end }}
{{- end }}