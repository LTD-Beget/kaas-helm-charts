{{- if false }}
  {{- $clientContext := set (deepCopy .) "Values" (set (deepCopy .Values) "environment" "client") -}}
  
  {{- include "capi-kubeadm-bootstrap.addon" . }}

  {{- .Files.Get "files/systemAddons/capi-kubeadm-bootstrap/values.yml" | nindent 0 }}

  {{- include "capi-kubeadm-bootstrap.phase" . }}
{{- end }}