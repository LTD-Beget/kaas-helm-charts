{{- if .Values.addons.grafanadashboards.enabled }}
  {{- $clientContext := set (deepCopy .) "Values" (set (deepCopy .Values) "environment" "client") -}}
  
  {{- if .Values.addons.system.enabled }}
    {{- include "grafana-dashboards.addon" . }}
  {{- end }}

  {{- .Files.Get "files/systemAddons/grafana-dashboards/values.yml" | nindent 0 }}

{{- end }}