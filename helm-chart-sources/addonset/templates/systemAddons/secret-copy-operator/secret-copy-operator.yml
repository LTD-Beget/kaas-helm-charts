{{- if false }}
  {{- $clientContext := set (deepCopy .) "Values" (set (deepCopy .Values) "environment" "client") -}}
  
  {{- include "secret-copy-operator.addon" . }}

  {{- .Files.Get "files/systemAddons/secret-copy-operator/values.yml" | nindent 0 }}
{{- end }}
