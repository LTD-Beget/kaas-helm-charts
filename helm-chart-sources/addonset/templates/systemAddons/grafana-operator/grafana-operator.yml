{{- if .Values.addons.grafanaoperator.enabled }}
  {{- $clientContext := set (deepCopy .) "Values" (set (deepCopy .Values) "environment" "client") -}}
  
  {{- if .Values.addons.system.enabled }}
    {{- include "grafana-operator.addon" . }}
  {{- end }}

  {{- .Files.Get "files/systemAddons/grafana-operator/values.yml" | nindent 0 }}

  {{- include "grafana-operator.phase" . }}

  {{- if eq .Values.environment "infra" }}
    {{- include "grafana-operator.phase" $clientContext }}
  {{- end }}
{{- end }}