{{- if and .Values.addons.clickhouseinserter.enabled .Values.addons.systeme.enabled }}
  {{- $clientContext := set (deepCopy .) "Values" (set (deepCopy .Values) "environment" "client") -}}

  {{- include "clickhouseinserter.addon" . }}

  {{- .Files.Get "files/systemAddons/clickhouseinserter/values.yml" | nindent 0 }}

  {{- include "clickhouseinserter.phase" . }}
{{- end }}
