{{- if and .Values.addons.crossplanefunctions.enabled .Values.addons.system.enabled }}
  {{- $clientContext := set (deepCopy .) "Values" (set (deepCopy .Values) "environment" "client") -}}

  {{- include "crossplane-functions.addon" . }}

  {{- .Files.Get "files/systemAddons/crossplane-functions/values.yml" | nindent 0 }}

  {{- include "crossplane-functions.phase" . }}

  {{- if eq .Values.environment "infra" }}
    {{- include "crossplane-functions.phase" $clientContext }}
  {{- end }}
{{- end }}
