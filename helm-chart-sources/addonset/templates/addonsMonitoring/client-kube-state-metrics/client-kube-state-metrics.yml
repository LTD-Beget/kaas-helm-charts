{{- if and .Values.addons.clientkubestatemetrics.enabled .Values.clientClusterEnabled }}
  {{- $clientContext := set (deepCopy .) "Values" (set (deepCopy .Values) "environment" "client") -}}

  {{- include "client-kube-state-metrics.addon" . }}

  {{- .Files.Get "files/addonsMonitoring/client-kube-state-metrics/values.yml" | nindent 0 }}

  {{- include "client-kube-state-metrics.phase" . }}
{{- end }}
