apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    cluster.x-k8s.io/v1beta1: v1beta1
    cluster.x-k8s.io/v1beta2: v1beta2
    clusterctl.cluster.x-k8s.io: ""
  name: {{ $.Values.companyPrefix }}clusters.infrastructure.cluster.x-k8s.io
  annotations:
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ $.Release.Name }}-{{ .Values.appSpec.applications.providerControllerManager.volumes.webhook.volume.name }}
spec:
  conversion:
    strategy: Webhook
    webhook:
      clientConfig:
        service:
          name: {{ $.Release.Name }}-{{ .Values.appSpec.applications.providerControllerManager.name }}-webhook
          namespace: {{ $.Release.Namespace }}
          path: /convert
      conversionReviewVersions:
      - v1
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: {{ $.Values.companyPrefix | title }}Cluster
    listKind: {{ $.Values.companyPrefix | title }}ClusterList
    plural: {{ $.Values.companyPrefix }}clusters
    singular: {{ $.Values.companyPrefix }}cluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Cluster infrastructure is ready
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: API Endpoint
      jsonPath: .spec.controlPlaneEndpoint
      name: Endpoint
      type: string
    name: v1beta2
    schema:
      openAPIV3Schema:
        description: {{ $.Values.companyPrefix | title }}Cluster is the Schema for the {{ $.Values.companyPrefix }}clusters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: {{ $.Values.companyPrefix | title }}ClusterSpec defines the desired state of {{ $.Values.companyPrefix | title }}Cluster.
            properties:
              controlPlaneEndpoint:
                description: APIEndpoint represents a reachable Kubernetes API endpoint.
                minProperties: 1
                properties:
                  host:
                    description: host is the hostname on which the API server is serving.
                    maxLength: 512
                    minLength: 1
                    type: string
                  port:
                    description: port is the port on which the API server is serving.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
              customerLogin:
                type: string
              loadBalancerSpec:
                properties:
                  displayName:
                    maxLength: 63
                    minLength: 3
                    pattern: ([a-z]([-a-z0-9]{0,61}[a-z0-9])?)?
                    type: string
                  healthcheck:
                    default: {}
                    properties:
                      healthcheckIntervalSec:
                        default: 3
                        type: integer
                      healthcheckThreshold:
                        default: 3
                        type: integer
                      healthcheckTimeoutSec:
                        default: 1
                        type: integer
                    type: object
                  listener:
                    properties:
                      address:
                        type: string
                      internal:
                        default: false
                        type: boolean
                      ports:
                        description: load balancer listener port mapping
                        items:
                          properties:
                            from:
                              default: 6443
                              format: int32
                              maximum: 65535
                              minimum: 1
                              type: integer
                            to:
                              default: 6443
                              format: int32
                              maximum: 65535
                              minimum: 1
                              type: integer
                            type:
                              default: generic
                              enum:
                              - generic
                              - controlPlane
                              type: string
                          required:
                          - from
                          - to
                          type: object
                        type: array
                    type: object
                  loadBalancerType:
                    default: external
                    enum:
                    - external
                    - internal
                    type: string
                  name:
                    maxLength: 63
                    minLength: 3
                    pattern: ([a-z]([-a-z0-9]{0,61}[a-z0-9])?)?
                    type: string
                required:
                - displayName
                - listener
                - name
                type: object
              region:
                type: string
            required:
            - customerLogin
            - loadBalancerSpec
            - region
            type: object
          status:
            description: {{ $.Values.companyPrefix | title }}ClusterStatus defines the observed state of {{ $.Values.companyPrefix | title }}Cluster.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              initialization:
                properties:
                  provisioned:
                    type: boolean
                type: object
              loadBalancer:
                properties:
                  backendId:
                    type: string
                  listenerAddress:
                    type: string
                  listenerPorts:
                    items:
                      format: int32
                      type: integer
                    type: array
                  loadBalancerId:
                    type: string
                  name:
                    type: string
                type: object
              ready:
                default: false
                type: boolean
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - description: Cluster infrastructure is ready
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: API Endpoint
      jsonPath: .spec.controlPlaneEndpoint
      name: Endpoint
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: {{ $.Values.companyPrefix | title }}Cluster is the Schema for the {{ $.Values.companyPrefix }}clusters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: {{ $.Values.companyPrefix | title }}ClusterSpec defines the desired state of {{ $.Values.companyPrefix | title }}Cluster.
            properties:
              controlPlaneEndpoint:
                description: APIEndpoint represents a reachable Kubernetes API endpoint.
                properties:
                  host:
                    description: host is the hostname on which the API server is serving.
                    maxLength: 512
                    type: string
                  port:
                    description: port is the port on which the API server is serving.
                    format: int32
                    type: integer
                type: object
              customerLogin:
                type: string
              loadBalancerSpec:
                properties:
                  displayName:
                    maxLength: 63
                    minLength: 3
                    pattern: ([a-z]([-a-z0-9]{0,61}[a-z0-9])?)?
                    type: string
                  healthcheck:
                    default: {}
                    properties:
                      healthcheckIntervalSec:
                        default: 3
                        type: integer
                      healthcheckThreshold:
                        default: 3
                        type: integer
                      healthcheckTimeoutSec:
                        default: 1
                        type: integer
                    type: object
                  listener:
                    properties:
                      address:
                        type: string
                      internal:
                        default: false
                        type: boolean
                      ports:
                        description: load balancer listener port mapping
                        items:
                          properties:
                            from:
                              default: 6443
                              format: int32
                              maximum: 65535
                              minimum: 1
                              type: integer
                            to:
                              default: 6443
                              format: int32
                              maximum: 65535
                              minimum: 1
                              type: integer
                            type:
                              default: generic
                              enum:
                              - generic
                              - controlPlane
                              type: string
                          required:
                          - from
                          - to
                          type: object
                        type: array
                    type: object
                  loadBalancerType:
                    default: external
                    enum:
                    - external
                    - internal
                    type: string
                  name:
                    maxLength: 63
                    minLength: 3
                    pattern: ([a-z]([-a-z0-9]{0,61}[a-z0-9])?)?
                    type: string
                required:
                - displayName
                - listener
                - name
                type: object
              region:
                type: string
            required:
            - customerLogin
            - loadBalancerSpec
            - region
            type: object
          status:
            description: {{ $.Values.companyPrefix | title }}ClusterStatus defines the observed state of {{ $.Values.companyPrefix | title }}Cluster.
            properties:
              conditions:
                description: Conditions provide observations of the operational state
                  of a Cluster API resource.
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This field may be empty.
                      maxLength: 10240
                      minLength: 1
                      type: string
                    reason:
                      description: |-
                        reason is the reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may be empty.
                      maxLength: 256
                      minLength: 1
                      type: string
                    severity:
                      description: |-
                        severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      maxLength: 32
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              loadBalancer:
                properties:
                  backendId:
                    type: string
                  listenerAddress:
                    type: string
                  listenerPorts:
                    items:
                      format: int32
                      type: integer
                    type: array
                  loadBalancerId:
                    type: string
                  name:
                    type: string
                type: object
              ready:
                default: false
                type: boolean
            required:
            - ready
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
