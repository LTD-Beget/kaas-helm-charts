{{- if and .Values.certificates.useExternalIssuer .Values.certificates.enabled }}
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ $.Release.Name }}-selfsigned-issuer
spec:
  selfSigned: {}
{{- end }}
