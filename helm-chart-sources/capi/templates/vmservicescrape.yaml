{{- if .Values.monitoring.vmServiceScrape.enabled }}
---
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: {{ include "capi.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "capi.labels" . | nindent 4 }}
spec:
  endpoints:
  - path: /metrics
    port: metrics
    scheme: HTTPS
    bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
    tlsConfig:
      serverName: localhost
  {{- if not .Values.certificates.useExternalIssuer }}
      insecureSkipVerify: true
  {{- end }}
  jobLabel: {{ include "capi.fullname" . }}
  selector:
    matchLabels:
      monitoring.in-cloud.io/service: {{ include "capi.fullname" . }}-metrics-service
      {{- include "capi.selectorLabels" . | nindent 6 }}
{{- end }}
