---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bcloud-capi.fullname" . }}-metrics-service
  labels:
    {{- include "bcloud-capi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metricsService.type }}
  ports:
    - port: {{ .Values.metricsService.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "bcloud-capi.selectorLabels" . | nindent 4 }}
