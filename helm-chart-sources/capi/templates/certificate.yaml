---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "capi.fullname" . }}-serving-cert
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "capi.labels" . | nindent 4 }}
spec:
  dnsNames:
    - {{ include "capi.fullname" . }}-webhook-service
    - {{ include "capi.fullname" . }}-webhook-service.{{ .Release.Namespace }}
    - {{ include "capi.fullname" . }}-webhook-service.{{ .Release.Namespace }}.svc
    - {{ include "capi.fullname" . }}-webhook-service.{{ .Release.Namespace }}.svc.{{ .Values.webhookService.domain }}
  issuerRef:
  {{- if .Values.certificates.useExternalIssuer }}
    {{ .Values.certificates.externalIssuer | toYaml | nindent 4 }}
  {{- else }}
    kind: ClusterIssuer
    name: {{ include "capi.fullname" . }}-selfsigned-issuer
  {{- end }}
  secretName: {{ include "capi.fullname" . }}-webhook-service-cert
  subject:
    organizations:
      - k8s-sig-cluster-lifecycle
