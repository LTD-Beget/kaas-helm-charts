apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./base.yaml

patches:
  - path: ./deployment.istiod.yaml
    target:
      kind: Deployment
      name: istiod

  - path: ./service.istiod.yaml
    target:
      kind: Service
      name: istiod
